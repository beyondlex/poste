{
  "name": "tree-sitter-poste-json",
  "version": "0.1.0",
  "description": "JSON with {{var}} variable references for Poste HTTP",
  "main": "grammar.js",
  "scripts": {
    "generate": "tree-sitter generate",
    "build": "tree-sitter build --wasm",
    "test": "tree-sitter test",
    "parse": "tree-sitter parse"
  },
  "tree-sitter": [
    {
      "scope": "source.poste_json",
      "injection-regex": "poste_json"
    }
  ]
}